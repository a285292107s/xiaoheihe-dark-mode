{
  "name": "xiaoheihe",
  "private": true,
  "version": "0.3.4",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-dark.mjs",
    "verify:idempotent": "node scripts/verify-idempotent.mjs",
    "verify:cascade": "node research/test-hover-cascade.mjs",
    "verify:ui": "node scripts/verify-ui.mjs",
    "verify:lifecycle": "node scripts/verify-lifecycle.mjs",
    "verify:preview": "node scripts/verify-preview.mjs",
    "verify:all": "npm run build && npm run verify:ui && npm run verify:lifecycle && npm run verify:preview && npm run verify && npm run verify:idempotent && npm run verify:cascade"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.11",
    "vite-plugin-monkey": "^8.0.0"
  }
}
