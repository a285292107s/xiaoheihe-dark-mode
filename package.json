{
  "name": "xiaoheihe",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-dark.mjs",
    "verify:idempotent": "node scripts/verify-idempotent.mjs",
    "verify:cascade": "node research/test-hover-cascade.mjs",
    "verify:all": "npm run build && npm run verify && npm run verify:idempotent && npm run verify:cascade"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.11",
    "vite-plugin-monkey": "^8.0.0"
  }
}
